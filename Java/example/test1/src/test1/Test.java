package test1;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.Scanner;

public class Test {
	/*
	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);
        SimpleDateFormat sdf = new SimpleDateFormat("HH:mm"); // 시간 형식 지정 (24시간 형식)

        try {
            System.out.print("출근 시간을 입력하세요 (HH:mm): ");
            String workTimeStr = scanner.nextLine();
            Date workTime = sdf.parse(workTimeStr);

            // 9시를 나타내는 Date 객체 생성
            Date nineAM = sdf.parse("09:00");

            if (workTime.compareTo(nineAM) > 0) {
                System.out.println("지각입니다.");
            } else {
                System.out.println("정시입니다.");
            }
        } catch (ParseException e) {
            System.out.println("잘못된 시간 형식입니다.");
        }
    }
*/
	
}

